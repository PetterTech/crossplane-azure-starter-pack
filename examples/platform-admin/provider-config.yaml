apiVersion: azure.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: azure-provider-creds
      key: credentials
# TODO: Replace the secret reference and validate the final provider package details for the target environment.
