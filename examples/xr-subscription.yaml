apiVersion: platform.example.org/v1alpha1
kind: Subscription
metadata:
  name: subscription-xr
  namespace: crossplane-system
spec:
  parameters:
    name: javazone-demo
    tags:
      environment: demo
